---
label: Form Block
hide_body: true
fields:
- name: block_title
  type: text
  config:
    required: false
  label: Block Title
- name: form_name
  type: text
  config:
    required: true
  label: Form Name
  description: HTML "name" attribute. Keep this to letters and underscores only (no
    spaces)
- name: fields
  type: blocks
  label: Form Fields
  template_types:
  - form-field
  config:
    min: 
    max: 
- name: submit_text
  type: text
  config:
    required: true
  label: Submit Text
  default: Submit
